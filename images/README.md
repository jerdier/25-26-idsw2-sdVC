# Imágenes

> | [Inicio](../README.md) | [Modelos UML](../modelosUML/README.md) | [Documentos](../documents/README.md) |
> |---|---|---|

SVGs exportados de los diagramas PlantUML. Genera los SVG con PlantUML desde los `.puml` en [modelosUML/](../modelosUML/README.md) y colócalos en la carpeta correspondiente.

---

## Análisis — `analisis/[caso]/colaboracion.svg`

| Caso de uso | SVG |
|-------------|-----|
| `crearUsuario()` | [ver](analisis/crearUsuario/colaboracion.svg) |
| `crearSesionClase()` | [ver](analisis/crearSesionClase/colaboracion.svg) |
| `crearSolicitudDispensa()` | [ver](analisis/crearSolicitudDispensa/colaboracion.svg) |
| `consultarUsuario()` | pendiente |
| `editarUsuario()` | pendiente |
| `editarSesionClase()` | pendiente |
| `cerrarSesionClase()` | pendiente |
| `registrarTomaAsistencia()` | pendiente |
| `exportarHistorialAsistencias()` | pendiente |
| `consultarDetalleAlumno()` | pendiente |
| `importarListasAlumnos()` | pendiente |
| `importarMatricula()` | pendiente |
| `consultarDetalleMatricula()` | pendiente |
| `exportarDispensas()` | pendiente |
| `consultarListaAlumnos()` | pendiente |
| `consultarSolicitudDispensa()` | pendiente |
| `editarSolicitudDispensa()` | pendiente |
| `guardarSolicitudDispensa()` | pendiente |

---

## Diseño — `diseño/[caso]/secuencia.svg`

| Caso de uso | SVG |
|-------------|-----|
| `crearUsuario()` | pendiente |
| `consultarUsuario()` | pendiente |
| `editarUsuario()` | pendiente |
| `crearSesionClase()` | pendiente |
| `editarSesionClase()` | pendiente |
| `cerrarSesionClase()` | pendiente |
| `registrarTomaAsistencia()` | pendiente |
| `exportarHistorialAsistencias()` | pendiente |
| `consultarDetalleAlumno()` | pendiente |
| `importarListasAlumnos()` | pendiente |
| `importarMatricula()` | pendiente |
| `consultarDetalleMatricula()` | pendiente |
| `exportarDispensas()` | pendiente |
| `crearSolicitudDispensa()` | pendiente |
| `editarSolicitudDispensa()` | pendiente |
| `consultarSolicitudDispensa()` | pendiente |
| `consultarListaAlumnos()` | pendiente |
| `guardarSolicitudDispensa()` | pendiente |
